#include "../common/logger.h"

const char key[] = "N*(JdjOSIDU*U(*&#$(JFL";

int main(int argc, char *argv[])
{
	do_log(append_log, NULL, "Hello world!\n");
	do_log2(read_log, NULL);
	do_log2(crypt_log, NULL);
	return 0;
}
